spring.config.import=optional:file:.env[.properties]
spring.application.name=${SPRING_APPLICATION_NAME:demo}

spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Root}
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/guardavidas?createDatabaseIfNotExist=true}

spring.jpa.database-platform=${JPA_DIALECT:org.hibernate.dialect.MySQLDialect}
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}

jwt.secret=${JWT_SECRET:RZJlhIsQr4HQSgjFdlzbcCF04AeQr2DbBPjhPJmuRKU3CFVbDhaRCvBsrR67oAgJ}
arquivamento.path=${ARQUIVAMENTO_PATH:C:/var}

spring.mail.host=${MAIL_HOST:smtp.hostinger.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:senai@participativo.com.br}
spring.mail.password=${MAIL_PASSWORD:0qo#7Nz6Z/}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.ssl.enable=${MAIL_SSL_ENABLE:true}
spring.mail.properties.mail.smtp.socketFactory.port=${MAIL_SOCKET_FACTORY_PORT:465}
spring.mail.properties.mail.smtp.socketFactory.class=${MAIL_SOCKET_FACTORY_CLASS:javax.net.ssl.SSLSocketFactory}
spring.mail.properties.mail.smtp.socketFactory.fallback=${MAIL_SOCKET_FACTORY_FALLBACK:false}
